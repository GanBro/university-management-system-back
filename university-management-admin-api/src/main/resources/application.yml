spring:
  datasource:
    url: jdbc:mysql://localhost:3306/university_management
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
  security:
    user:
      name: admin
      password: admin123
mybatis-plus:
  type-aliases-package: com.wubo.api.entity
